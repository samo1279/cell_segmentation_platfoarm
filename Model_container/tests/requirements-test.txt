# Test-only dependencies for Model_container unit tests.
# These are intentionally separate from requirements.txt:
# - No cellpose / torch / torchvision — the test suite stubs cellpose at import time.
# - Keeps the CI test job fast (< 30 s install) and runnable without a GPU.
pytest==8.3.5
httpx==0.27.0
fastapi[standard]
numpy
imageio
tifffile
Pillow
anyio[trio]
